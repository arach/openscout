# scout

User-facing CLI scaffold for OpenScout.

This package is separate from `scout-dev`:

- `scout` is the long-term product CLI
- `scout-dev` is the repo-local native developer wrapper

## Current Commands

```bash
scout --help
scout version
scout doctor
scout init
scout app launch
scout app status
```
