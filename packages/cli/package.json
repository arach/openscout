{
  "name": "@openscout/cli",
  "version": "0.1.1",
  "description": "Public Scout CLI for the OpenScout operator surface",
  "license": "UNLICENSED",
  "type": "module",
  "bin": {
    "scout": "./bin/scout.mjs"
  },
  "files": [
    "bin",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "cd ../runtime && bun run build && cd ../cli && mkdir -p dist && bun build ./src/main.ts --target=node --outfile ./dist/main.mjs",
    "dev": "node ./bin/scout.mjs",
    "test:happy": "node --test test/*.test.mjs",
    "prepack": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@openscout/runtime": "workspace:*"
  }
}
