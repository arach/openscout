{
  "name": "@openscout/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Local runtime foundation for the OpenScout control plane",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "broker": "bun run src/broker-daemon.ts",
    "discover": "bun run src/broker-daemon.ts",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@openscout/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
